# Composite action: Module 2 (triage reply)
# PRD §2 module 2. See docs/composite-action-spec.md for the full interface.
#
# Engine-agnostic wrapper (PRD §4). With engine=claude it uses
# anthropics/claude-code-action@v1; with engine=codex it uses openai/codex-action@v1.
#
# claude-code-action@v1 API:
#   - prompt, settings (JSON), claude_args (CLI flags), anthropic_api_key, github_token
#   - Structured output: pass `--json-schema '<json>'` in claude_args; read
#     `structured_output` output (JSON string).
#
# GLM (Zhipu AI) passthrough: when `anthropic-base-url` input is non-empty, the
# claude branch injects it as ANTHROPIC_BASE_URL env on the action step.
# claude-code CLI honors that env var, so requests route to the Zhipu
# Anthropic-compatible endpoint without forking the action.
#
# REQUIRED PERMISSIONS (calling workflow must grant):
#   contents: read       (read CLAUDE.md, prompts)
#   issues:   write      (apply triage label) — PRD §7 S1
#
# This action does NOT post the comment — the caller decides whether to post.

name: "Module 2 — Triage reply"
description: "First-response, dedupe, clarification, routing label for newly opened Issue (PRD §2 module 2)."

inputs:
  issue-number:
    description: "Issue number to triage"
    required: true
  repo-token:
    description: "GITHUB_TOKEN with issues:write"
    required: true
  api-key:
    description: "Engine provider API key (ZHIPU_API_KEY when using GLM passthrough)"
    required: true
  anthropic-base-url:
    description: "Anthropic-compatible API base URL (e.g. https://open.bigmodel.cn/api/anthropic for GLM). Empty = Anthropic default."
    required: false
    default: ""
  engine:
    description: "LLM engine (claude | codex)"
    required: false
    default: "claude"
  model:
    description: "Model ID override. Empty = engine default (or whatever GLM proxy serves)."
    required: false
    default: ""
  max-turns:
    description: "Agent turn cap"
    required: false
    default: "6"
  context-budget-paths:
    description: "Space-sep path allowlist (currently advisory; not enforced by action)"
    required: false
    default: ""
  dry-run:
    description: "If true, do not apply labels (comment posting is caller's responsibility)"
    required: false
    default: "false"

outputs:
  stage:
    description: "Lifecycle stage reached (triage)"
    value: ${{ steps.emit.outputs.stage }}
  audit-comment-url:
    description: "URL of the audit comment (empty — comment is posted by the caller workflow)"
    value: ${{ steps.emit.outputs.audit-comment-url }}
  decision:
    description: "reply | work"
    value: ${{ steps.parse.outputs.decision }}
  comment_body:
    description: "Body of the comment to post on the issue (multi-line markdown)"
    value: ${{ steps.parse.outputs.comment_body }}
  suggested_labels:
    description: "Comma-sep suggested labels"
    value: ${{ steps.parse.outputs.suggested_labels }}
  confidence:
    description: "float [0,1] — claude's confidence in the decision"
    value: ${{ steps.parse.outputs.confidence }}
  workload_class:
    description: "M3/M8 (pipeline-redesign-v3): trivial | standard | complex. Defaults to 'standard' if engine omits. M8 wires this into triage-issue.yml routing — trivial/standard auto-apply accepted-by-claude (S2 amendment), complex routes to clarify loop."
    value: ${{ steps.parse.outputs.workload_class }}

runs:
  using: composite
  steps:
    # Fetch issue title/body/labels and inject into the prompt. Claude can't
    # fetch issues itself (no Bash/Network in the allow-list), so we provide
    # the body as context.
    - name: Fetch issue context
      id: issue
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.repo-token }}
        NUMBER: ${{ inputs.issue-number }}
      run: |
        set -eu
        json=$(gh issue view "$NUMBER" --repo "$GITHUB_REPOSITORY" --json title,body,labels,url)
        title=$(printf '%s' "$json" | jq -r '.title')
        url=$(printf '%s' "$json"   | jq -r '.url')
        body=$(printf '%s' "$json"  | jq -r '.body // "(empty)"')
        labels=$(printf '%s' "$json" | jq -r '.labels | map(.name) | join(", ")')
        if [ -z "$labels" ]; then labels="(none)"; fi

        # Multi-line values via heredoc delimiters
        {
          echo "title<<TITLE_EOF"
          printf '%s\n' "$title"
          echo "TITLE_EOF"
          echo "url=$url"
          echo "labels=$labels"
          echo "body<<BODY_EOF"
          printf '%s\n' "$body"
          echo "BODY_EOF"
        } >> "$GITHUB_OUTPUT"

    # Build settings JSON (model only included if non-empty) and the
    # --json-schema arg. Both are passed verbatim to claude-code-action.
    - name: Build claude config
      id: cfg
      shell: bash
      env:
        MODEL_INPUT: ${{ inputs.model }}
        MAX_TURNS: ${{ inputs.max-turns }}
      run: |
        set -eu
        # settings JSON: permissions + maxTurns; model only if non-empty.
        # PR #132: allow `Bash(gh issue:*)` so Claude can self-post its
        # triage comment with hidden JSON block. GLM passthrough does not
        # reliably honor --json-schema strict mode; we instead parse the
        # hidden TRIAGE_VERDICT block from the issue comment.
        if [ -n "$MODEL_INPUT" ]; then
          settings=$(jq -c -n \
            --arg m "$MODEL_INPUT" \
            --argjson t "$MAX_TURNS" \
            '{permissions:{allow:["Read","Grep","Glob","Bash(gh issue:*)"],deny:["Write","Edit","Bash(git:*)","Bash(gh pr:*)","Bash(gh repo:*)","Bash(rm:*)"]}, maxTurns:$t, model:$m}')
        else
          settings=$(jq -c -n \
            --argjson t "$MAX_TURNS" \
            '{permissions:{allow:["Read","Grep","Glob","Bash(gh issue:*)"],deny:["Write","Edit","Bash(git:*)","Bash(gh pr:*)","Bash(gh repo:*)","Bash(rm:*)"]}, maxTurns:$t}')
        fi
        {
          echo "settings<<SETTINGS_EOF"
          echo "$settings"
          echo "SETTINGS_EOF"
        } >> "$GITHUB_OUTPUT"

        # JSON schema for structured output — compact, single-line
        # M3/M8 (pipeline-redesign-v3): workload_class is optional (not in `required`)
        # so older engines that don't emit it still validate; extract.sh defaults
        # missing values to "standard". M8 wires workload_class into the triage
        # routing decision in triage-issue.yml (replaced the binary
        # AUTO_ACCEPT_ENABLED repo var).
        schema=$(jq -c . <<'JSON'
        {
          "type": "object",
          "required": ["decision", "comment_body", "suggested_labels", "confidence"],
          "properties": {
            "decision": {"type": "string", "enum": ["reply", "work"]},
            "comment_body": {"type": "string", "minLength": 10},
            "suggested_labels": {"type": "array", "items": {"type": "string"}},
            "confidence": {"type": "number", "minimum": 0, "maximum": 1},
            "workload_class": {"type": "string", "enum": ["trivial", "standard", "complex"]}
          },
          "additionalProperties": false
        }
        JSON
        )
        # Wrap in single quotes for shell; the schema itself has no single quotes.
        echo "schema='${schema}'" >> "$GITHUB_OUTPUT"

    - name: Triage via Claude (or GLM via base-url passthrough)
      if: inputs.engine == 'claude'
      id: claude-run
      uses: anthropics/claude-code-action@v1
      env:
        # Empty string → env unset → claude-code CLI uses Anthropic default.
        # Non-empty → CLI routes to the given Anthropic-compatible endpoint.
        ANTHROPIC_BASE_URL: ${{ inputs.anthropic-base-url }}
      with:
        anthropic_api_key: ${{ inputs.api-key }}
        github_token: ${{ inputs.repo-token }}
        prompt: |
          You are the triage module of the GithubAutoDev pipeline.
          A new Issue was just opened.

          Title: ${{ steps.issue.outputs.title }}
          URL: ${{ steps.issue.outputs.url }}
          Issue number: ${{ inputs.issue-number }}
          Existing labels: ${{ steps.issue.outputs.labels }}

          Issue body:
          ```
          ${{ steps.issue.outputs.body }}
          ```

          Read CLAUDE.md for project context (only if the issue references
          directories you need to inspect; otherwise skip). Do NOT scan the
          whole repo.

          Decide ONE of:
            - "reply": the issue needs clarification, is a duplicate, is out of
              scope, or is a user-error. Your comment_body should ask the question
              or explain the resolution.
            - "work":  the issue is a legitimate feature/bug that should enter the
              development pipeline. Your comment_body should be a brief
              acknowledgement + summary of what you understood the ask to be.

          Never print tokens, API keys, or environment variable values (PRD §7 S4).

          ## Output protocol (mandatory)

          You MUST post your triage decision as a comment on the issue using:

          ```
          gh issue comment ${{ inputs.issue-number }} --repo "$GITHUB_REPOSITORY" --body "<BODY>"
          ```

          The `<BODY>` must be a single shell-safe string with this structure:

          ```
          🤖 **Triage decision**

          <one-paragraph human-readable summary — what you understood, your decision>

          <!-- TRIAGE_VERDICT
          {"decision":"<reply|work>","comment_body":"<markdown for the user-facing reply>","suggested_labels":[<array of strings>],"confidence":<0.0-1.0>,"workload_class":"<trivial|standard|complex>"}
          -->
          ```

          Rules for the JSON inside `<!-- TRIAGE_VERDICT ... -->`:
          - MUST be valid JSON on a single line (no newlines inside the JSON)
          - `decision`: exactly "reply" or "work"
          - `comment_body`: >= 10 chars, plain text or markdown
          - `suggested_labels`: JSON array of label name strings (empty `[]` if none)
          - `confidence`: float in [0, 1]
          - `workload_class`: "trivial" (low-risk, single-file, clear AC), "standard" (one module, clear AC), or "complex" (cross-module, ambiguous AC)
          - No trailing commas, no comments inside JSON

          After posting the comment, respond with a one-line confirmation.
        settings: ${{ steps.cfg.outputs.settings }}

    - name: Triage via Codex
      if: inputs.engine == 'codex'
      id: codex-run
      uses: openai/codex-action@v1
      with:
        openai_api_key: ${{ inputs.api-key }}
        github_token: ${{ inputs.repo-token }}
        prompt: |
          You are the triage module. Triage Issue #${{ inputs.issue-number }} per CLAUDE.md.
          Output JSON: { decision: "reply"|"work", comment_body, suggested_labels, confidence }.
          Never print secrets.

    # PR #132: Claude posts triage decision as an issue comment containing
    # a hidden <!-- TRIAGE_VERDICT {...} --> JSON block. We pull the latest
    # comment and parse the JSON. GLM passthrough does not reliably honor
    # --json-schema strict mode, so we use this prompt+parse pattern
    # (mirrors review.yml REVIEW_VERDICT block design).
    - name: Parse engine output
      id: parse
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.repo-token }}
        ISSUE_NUMBER: ${{ inputs.issue-number }}
      run: |
        set -euo pipefail
        # Fetch the latest comment on the issue (Claude just posted it).
        body=$(gh issue view "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" \
          --json comments --jq '.comments[-1].body // ""' 2>/dev/null || echo "")
        if [ -z "$body" ]; then
          echo "::error::No triage comment found on issue #$ISSUE_NUMBER."
          echo "::error::Claude may have failed to post its TRIAGE_VERDICT block."
          exit 1
        fi
        export STRUCTURED="$body"
        ./.github/actions/triage/extract.sh

    - name: Emit common outputs
      id: emit
      shell: bash
      env:
        DECISION: ${{ steps.parse.outputs.decision }}
        DRY_RUN: ${{ inputs.dry-run }}
      run: |
        echo "stage=triage" >> "$GITHUB_OUTPUT"
        echo "audit-comment-url=" >> "$GITHUB_OUTPUT"
        if [ "$DRY_RUN" = "true" ]; then
          echo "Dry-run: skipping label application (comment posting is caller's job)"
        fi
