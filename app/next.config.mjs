/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Auth (Issue #3), webhooks (Issue #4), and image domains will land in later issues.
};

export default nextConfig;
